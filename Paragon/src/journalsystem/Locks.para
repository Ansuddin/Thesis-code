public class Locks{

	public static !(Policy.highD + Policy.trusted) lock IsDoctor; // The confidentiality policy does not effect anything
	public static !(Policy.highD + Policy.trusted) lock IsReferred(Doctor); // Proberbly opened in Hospital class.
	// Can only be opended when IsReferred is open.
	public static !(Policy.highD + Policy.trusted) lock Referred;
	public static !(Policy.highD + Policy.trusted) lock OnlyMatrix;
}